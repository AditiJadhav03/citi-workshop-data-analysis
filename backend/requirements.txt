# Data pipeline dependencies.
#
# Install with:
#     pip install -r requirements.txt
#
# PySpark 4.x requires Java 17 or newer on the PATH.

# --- Processing engine -----------------------------------------------------
pyspark==4.2.0

# --- PostgreSQL ------------------------------------------------------------
# psycopg2 is used for connection checks, schema creation and row counts.
# Spark itself talks to PostgreSQL over the JDBC driver, which is fetched
# automatically from Maven via spark.jars.packages (see config.spark_conf).
psycopg2-binary==2.9.9

# --- Analytics and notebook ------------------------------------------------
pandas==2.2.3
pyarrow==17.0.0
matplotlib==3.9.2
jupyter==1.1.1
notebook==7.2.2

# --- Cloud (only needed when IS_LOCAL=false) -------------------------------
boto3==1.35.36
s3fs==2024.10.0

# --- Testing ---------------------------------------------------------------
pytest==8.3.3
pytest-cov==5.0.0